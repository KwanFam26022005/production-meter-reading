/**
 * SceneFilter — Contextual spatial filter component.
 * Direct export connecting the presentation layer to the spatial filter engine.
 */
export { FilterPopover as SceneFilter } from '../components/FilterPopover';
